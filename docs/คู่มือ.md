# 🏥 คู่มือการใช้งานระบบ Medical AI Q&A

## 📋 เกี่ยวกับระบบ

ระบบ **Medical AI Q&A** เป็นระบบปัญญาประดิษฐ์ทางการแพทย์ที่ให้คำปรึกษาด้านสุขภาพโดยใช้เทคโนโลยีขั้นสูง:

- 🤖 **AI Model**: T5-Small สำหรับวินิจฉัยโรคจากอาการ
- 🔍 **RAG Technology**: ค้นหาความรู้ทางการแพทย์จากฐานข้อมูล
- ⚡ **FastAPI**: API เร็วและปลอดภัย
- 🔄 **N8N Workflow**: ระบบอัตโนมัติสำหรับการทำงาน
- 💾 **Vector Database**: ChromaDB สำหรับการค้นหาเชิงความหมาย

---

## 🔧 ความต้องการของระบบ

### ระบบปฏิบัติการ
- ✅ Windows 10/11
- ✅ Linux (Ubuntu 18.04+)
- ✅ macOS (10.15+)

### ซอฟต์แวร์ที่จำเป็น
- **Python 3.8+** (แนะนำ Python 3.11)
- **Docker Desktop** (สำหรับ N8N - ไม่บังคับ)
- **Git** (สำหรับโคลนโปรเจกต์)

### ฮาร์ดแวร์ขั้นต่ำ
- **RAM**: 8GB+
- **พื้นที่ฮาร์ดดิสก์**: 5GB+
- **GPU**: NVIDIA GPU (ไม่บังคับ แต่แนะนำสำหรับประสิทธิภาพที่ดีขึ้น)

---

## 🚀 การติดตั้งและตั้งค่า

### ขั้นตอนที่ 1: ดาวน์โหลดโปรเจกต์

```bash
# โคลนโปรเจกต์จาก GitHub
git clone https://github.com/your-repo/medical-ai-system.git
cd medical-ai-system
```

### ขั้นตอนที่ 2: ติดตั้ง Python Virtual Environment

```bash
# สร้าง virtual environment
python -m venv .venv

# เปิดใช้งาน virtual environment
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate
```

### ขั้นตอนที่ 3: ติดตั้ง Dependencies

```bash
# ติดตั้งแพ็คเกจทั้งหมด
pip install -r requirements.txt

# หรือติดตั้งแบบ development
pip install -e .
```

### ขั้นตอนที่ 4: ตรวจสอบโมเดล AI

ระบบต้องการโมเดล T5-Small ที่ผ่านการเทรนแล้ว:

```bash
# ตรวจสอบโมเดล
dir models\
```

หากไม่มีโมเดล ให้เทรนโมเดลก่อน:

```bash
# เทรนโมเดล T5
python scripts\Main_8_Train_T5_Medical.py
```

---

## 🎯 การรันระบบ

### วิธีที่ 1: รันทั้งระบบด้วยคำสั่งเดียว (แนะนำ)

```bash
# คลิกที่ไฟล์ START-ALL.bat หรือรันคำสั่งนี้
START-ALL.bat
```

ระบบจะ:
1. ✅ ตรวจสอบ Docker (สำหรับ N8N)
2. ✅ ตรวจสอบ Python environment
3. ✅ ตรวจสอบโมเดล T5
4. ✅ รัน N8N Workflow Server
5. ✅ รัน Medical API Server
6. ✅ เปิด Web Interface

### วิธีที่ 2: รันแยกส่วน

```bash
# รันเฉพาะ API
start-api.bat

# รันเฉพาะ N8N (ต้องมี Docker)
start-n8n.bat
```

### วิธีที่ 3: รันด้วย Python โดยตรง

```bash
# รัน API server
python run_api.py --host 0.0.0.0 --port 8000

# รันด้วย uvicorn โดยตรง
uvicorn src.api.integrated_medical_api:app --reload --host 0.0.0.0 --port 8000
```

---

## 🌐 การเข้าถึงระบบ

หลังจากรันระบบแล้ว สามารถเข้าถึงได้ที่:

| บริการ | URL | คำอธิบาย |
|---------|-----|-----------|
| **API Documentation** | http://localhost:8000/docs | Swagger UI สำหรับทดสอบ API |
| **API Health Check** | http://localhost:8000/health | ตรวจสอบสถานะระบบ |
| **API Status** | http://localhost:8000/status | ข้อมูลสถานะโดยละเอียด |
| **N8N Workflow** | http://localhost:5678 | ระบบ workflow automation |
| **Web Interface** | http://localhost:8000 (หรือไฟล์ HTML) | อินเทอร์เฟซผู้ใช้ |

---

## 💬 การใช้งานระบบ

### วิธีที่ 1: ผ่าน Web Interface

1. เปิดเบราว์เซอร์ไปที่ http://localhost:8000
2. พิมพ์อาการที่ต้องการปรึกษา เช่น:
   - "มีไข้ ไอ มีเสมหะ"
   - "ปวดหัว เวียนหัว คลื่นไส้"
   - "เจ็บหน้าอก หายใจลำบาก"

### วิธีที่ 2: ผ่าน API โดยตรง

#### ใช้ cURL

```bash
# ส่งคำถามไปยังระบบ
curl -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d "{\"question\": \"มีไข้สูง ไอแห้ง หายใจลำบาก\"}"
```

#### ใช้ Python

```python
import requests

# ส่งคำถาม
response = requests.post(
    "http://localhost:8000/ask",
    json={"question": "ปวดท้อง ท้องเสีย อาเจียน"}
)

print(response.json())
```

### วิธีที่ 3: ผ่าน Swagger UI

1. เปิด http://localhost:8000/docs
2. คลิกที่ endpoint `/ask`
3. คลิก "Try it out"
4. พิมพ์อาการในช่อง "question"
5. คลิก "Execute"

---

## 📝 API Reference

### POST /ask
ส่งคำถามทางการแพทย์เพื่อรับคำแนะนำ

**Request Body:**
```json
{
  "question": "อาการที่ต้องการปรึกษา"
}
```

**Response:**
```json
{
  "answer": "คำแนะนำทางการแพทย์",
  "confidence": 0.85,
  "sources": ["แหล่งข้อมูลที่ใช้"]
}
```

**ตัวอย่าง:**
```bash
curl -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d "{\"question\": \"มีไข้ ไอ หายใจลำบาก\"}"
```

### GET /health
ตรวจสอบสถานะระบบ

**Response:**
```json
{
  "status": "healthy",
  "timestamp": "2025-10-26T10:00:00Z"
}
```

### GET /status
ดูข้อมูลสถานะโดยละเอียด

**Response:**
```json
{
  "api_version": "1.0.0",
  "model_loaded": true,
  "model_name": "T5-Small Diagnosis",
  "uptime": "2h 30m"
}
```

---

## 🧪 การทดสอบระบบ

### ทดสอบด้วย pytest

```bash
# รันการทดสอบทั้งหมด
python -m pytest tests/ -v

# รันเฉพาะ API tests
python -m pytest tests/test_api.py -v

# รันเฉพาะ model tests
python -m pytest tests/test_models.py -v
```

### ทดสอบด้วย scripts

```bash
# ทดสอบ API ด้วย script
python test_analyze_api.py

# ทดสอบ LangChain service
python test_langchain.py
```

### ทดสอบด้วย batch files

```bash
# ทดสอบ T5 model
test-t5.bat

# ทดสอบ LangChain
test-langchain.bat
```

---

## 🔧 การแก้ปัญหา

### ปัญหาที่พบบ่อย

#### 1. "Python virtual environment not found"

**สาเหตุ:** ไม่มี virtual environment
**วิธีแก้:**
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

#### 2. "T5-Small model not found"

**สาเหตุ:** ไม่มีโมเดลที่เทรนแล้ว
**วิธีแก้:**
```bash
python scripts\Main_8_Train_T5_Medical.py
```

#### 3. "Docker is not running"

**สาเหตุ:** Docker Desktop ไม่ทำงาน
**วิธีแก้:**
- เปิด Docker Desktop
- รอให้ Docker เริ่มทำงาน
- รัน START-ALL.bat อีกครั้ง

#### 4. "Port 8000 already in use"

**สาเหตุ:** มีโปรแกรมอื่นใช้ port 8000
**วิธีแก้:**
```bash
# เปลี่ยน port
python run_api.py --port 8001
```

#### 5. "CUDA out of memory"

**สาเหตุ:** GPU memory ไม่เพียงพอ
**วิธีแก้:**
- ลด batch size ใน config
- ใช้ CPU แทน GPU
- ปิดโปรแกรมอื่นที่ใช้ GPU

### ตรวจสอบ logs

```bash
# ดู logs ของ API
type logs\fastapi.log

# ดู logs ของ model training
type logs\model_training.log

# ดู logs ของ N8N
docker logs n8n
```

### รีสตาร์ทระบบ

```bash
# หยุดระบบทั้งหมด
taskkill /f /im python.exe
docker-compose down

# รีสตาร์ท
START-ALL.bat
```

---

## 📊 การตรวจสอบประสิทธิภาพ

### ดูสถิติโมเดล

```bash
# รัน evaluation
python scripts\Main_9_Model_Evaluation.py

# ดู confusion matrix
python scripts\evaluate-confusion-matrix.bat
```

### ตรวจสอบ API performance

```bash
# ทดสอบ response time
curl -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d "{\"question\": \"test\"}" \
  -w "@curl-format.txt"
```

---

## 🔄 การอัปเดตระบบ

### อัปเดต dependencies

```bash
# อัปเดตแพ็คเกจ
pip install -r requirements.txt --upgrade

# อัปเดตโมเดล
python scripts\Main_8_Train_T5_Medical.py
```

### อัปเดต codebase

```bash
# Pull latest changes
git pull origin main

# รีสตาร์ทระบบ
START-ALL.bat
```

---

## 📞 การสนับสนุน

### ช่องทางการติดต่อ

- 📧 **Email**: support@medical-ai.com
- 💬 **Discord**: https://discord.gg/medical-ai
- 📖 **Documentation**: https://docs.medical-ai.com
- 🐛 **Issues**: https://github.com/your-repo/issues

### ข้อมูลเพิ่มเติม

- 📚 **API Docs**: http://localhost:8000/docs
- 🔬 **Technical Docs**: docs/TECHNICAL_IMPLEMENTATION.md
- 🐳 **Docker Guide**: docs/DOCKER_DEPLOYMENT_GUIDE.md
- 🔍 **Troubleshooting**: docs/TROUBLESHOOTING_GUIDE.md

---

## ⚠️ คำเตือนทางการแพทย์

**ระบบนี้เป็นเครื่องมือช่วยเหลือ ไม่ใช่การวินิจฉัยทางการแพทย์**

- ❌ ไม่ใช่การทดแทนแพทย์
- ❌ ไม่ใช่การให้คำวินิจฉัยทางการแพทย์
- ❌ ไม่ใช่การสั่งยา
- ✅ ใช้เพื่อการศึกษาและคำแนะนำเบื้องต้น
- ✅ ควรปรึกษาแพทย์เสมอ

**ในกรณีฉุกเฉิน โทร 1669 หรือไปโรงพยาบาลทันที**

---

## 📝 License

ระบบนี้อยู่ภายใต้ MIT License - ดูรายละเอียดใน `LICENSE`

---

*อัปเดตล่าสุด: 26 ตุลาคม 2025*</content>
<parameter name="filePath">d:\AI-PROJECT\คู่มือ.md