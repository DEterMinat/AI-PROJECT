# คำสั่งสำหรับ Push โปรเจกต์ขึ้น GitHub

## ขั้นตอนที่ 1: สร้าง GitHub Repository

1. เข้าเว็บไซต์ https://github.com
2. คลิกปุ่ม **"New repository"**
3. ตั้งชื่อ repository เช่น: `medical-ai-system`
4. คำอธิบาย: `Medical AI Q&A System with T5-Small and RAG`
5. เลือก **Public** หรือ **Private**
6. **ไม่ต้อง** ติ๊ก "Add a README file"
7. คลิก **"Create repository"**

## ขั้นตอนที่ 2: Push โปรเจกต์ขึ้น GitHub

รันคำสั่งต่อไปนี้ใน Command Prompt หรือ PowerShell:

```bash
# เปลี่ยน YOUR_USERNAME เป็นชื่อ GitHub ของคุณ
# เปลี่ยน YOUR_REPO_NAME เป็นชื่อ repository ที่สร้าง

git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

## ตัวอย่างสำหรับคุณ:

ถ้าชื่อ GitHub ของคุณคือ `tanakorn` และชื่อ repo คือ `medical-ai-system`:

```bash
git remote add origin https://github.com/tanakorn/medical-ai-system.git
git branch -M main
git push -u origin main
```

## ขั้นตอนที่ 3: ยืนยันการ Upload

หลังจาก push เสร็จแล้ว:
- เข้าไปดูที่ https://github.com/YOUR_USERNAME/YOUR_REPO_NAME
- คุณจะเห็นไฟล์ทั้งหมดของโปรเจกต์อยู่บน GitHub แล้ว

## 📝 หมายเหตุสำคัญ

- ไฟล์ที่อยู่ใน `.gitignore` จะไม่ถูก upload (เช่น โมเดล ข้อมูล logs)
- โปรเจกต์นี้มี 126 ไฟล์ที่พร้อมสำหรับ GitHub
- ตรวจสอบให้แน่ใจว่าไม่มีข้อมูลสำคัญ (passwords, API keys) ในโค้ด

## 🔧 ถ้ามีปัญหา

ถ้าเจอ error เกี่ยวกับ authentication:
```bash
# ใช้ Personal Access Token แทน password
git remote set-url origin https://YOUR_USERNAME:YOUR_TOKEN@github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
```

---

*สร้างเมื่อ: 26 ตุลาคม 2025*